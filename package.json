{
  "name": "stm-detector",
  "version": "1.1.0",
  "description": "Detect device type",
  "main": "lib/detector.js",
  "scripts": {
    "test": "npm run compile && mochify",
    "watch-test": "npm run compile && mochify -w",
    "compile": "rimraf lib/* && babel src -d lib",
    "prepublish": "npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SmartTeleMax/stm-detector.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/SmartTeleMax/stm-detector/issues"
  },
  "homepage": "https://github.com/SmartTeleMax/stm-detector#readme",
  "dependencies": {
    "jquery": ">= 1.11.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.17.0",
    "babel-preset-es2015": "^6.16.0",
    "chai": "^3.5.0",
    "mochify": "^2.18.1",
    "rimraf": "^2.5.4"
  }
}
